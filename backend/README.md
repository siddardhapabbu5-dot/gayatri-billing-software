# API moved — do not develop here

The Spring Boot API is maintained as a **separate project** (not mixed with the React UI):

**`C:\Users\harsh\OneDrive\Documents\Desktop\Gayatri-VHMS-API`**

```bash
cd "C:\Users\harsh\OneDrive\Documents\Desktop\Gayatri-VHMS-API"
docker compose up -d
mvn spring-boot:run
```

This `backend/` folder under the UI is only a pointer. Use **Gayatri-VHMS-API** for all API work.
