node_modules
dist
.DS_Store
*.local
.env
.env.*
!.env.example

# Gallery videos are committed (compressed ~14MB total) so Railway can serve them.

# Spring Boot
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar

# Android build + signing key — never commit
apk-out/
